# Copia este archivo como `.env.local`.
#
# Sin ninguna variable, la app arranca en MODO DEMO: no llama a Anthropic,
# no necesita clave y devuelve una respuesta de ejemplo.
#
# Para usar la API real, define ambas variables:
NEXT_PUBLIC_DEMO_MODE=false
ANTHROPIC_API_KEY=sk-ant-...
